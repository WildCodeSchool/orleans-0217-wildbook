
    <div class="col-xs-3 col-sm-3 col-md-3 col-lg-3">
        {% for homeWild in homeWilder %}
        <div class="thumbnail">
            <img src="{{ asset('uploads/'~ homeWild.wilder.profilPicture|basename ) }}"
                 alt="{{ homeWild.wilder.firstname }}{{ homeWild.wilder.lastname }}" class="homeWilder">
            <div class="caption">
                <h3>{{ homeWild.wilder.firstname }}{{ homeWild.wilder.lastname }}</h3>
                <p>
                    {{ homeWild.description }}
                </p>
                <p>
                    <a href="{{ path('profile_wilder', {'id': homeWild.wilder.id}) }}" class="btn btn-primary">Découvrir</a>
                </p>
            </div>
        </div>
    </div>
    {% endfor %}
