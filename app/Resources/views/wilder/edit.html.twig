{% extends 'base_admin.html.twig' %}
{#{% form_theme edit_form 'bootstrap_3_layout.html.twig' %}#}
{#{% form_theme edit_form 'form_div_layout.html.twig' %}#}
{#{% form_theme edit_form 'form_table_layout.html.twig' %}#}
{% form_theme edit_form 'bootstrap_3_horizontal_layout.html.twig' %}
{#{% form_theme edit_form 'foundation_5_layout.html.twig' %}#}

{% block body %}

    <div class="container-fluid">


        <h3>Wilder: {{ wilder.lastname }}</h3>
        <div class="form-group">
            {{ form_start(edit_form) }}
            {{ form_widget(edit_form) }}
        </div>
        <div class="form-group">
            <input type="submit" class="btn btn-success" value="Edit" />
        </div>
        {{ form_end(edit_form) }}

        <div class="form-group">
            {{ form_start(delete_form) }}
            <input type="submit" class="btn btn-danger" value="Delete">
            {{ form_end(delete_form) }}
        </div>

        <div class="form-group">
            <a href="{{ path('wilder_index') }}" class="btn btn-primary">Retour à la liste</a>
        </div>


    </div>
{% endblock body %}