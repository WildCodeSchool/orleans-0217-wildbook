{% extends 'base.html.twig' %}

{% block body %}
    <h1>Campusmanager</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ campusManager.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('campusmanager_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('campusmanager_edit', { 'id': campusManager.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
