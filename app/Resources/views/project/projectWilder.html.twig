{% form_theme form 'bootstrap_3_layout.html.twig' %}
{{ form_start(form, {'action':path('add_wilder_project', {'id':project.id})}) }}
{{ form_row(form.wilder) }}
<input type="submit" class="btn btn-success" value="Ajouter"/>

{{ form_end(form) }}
