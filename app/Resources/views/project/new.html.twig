{% extends 'layout_admin.html.twig' %}

{% form_theme form 'bootstrap_3_layout.html.twig' 'jquery.collection.html.twig' %}


{% block content %}
    <div class="container-fluid">

        <h1>Project creation</h1>

        {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.customer) }}
        {{ form_row(form.school) }}
        {{ form_row(form.date) }}
        {{ form_row(form.status) }}
        {{ form_row(form.category) }}
        {{ form_row(form.summary) }}
        {{ form_row(form.description) }}
        {{ form_row(form.path) }}
        {{ form_row(form.tags) }}
        {{ form_row(form.technologies) }}
        {{ form_row(form.languages) }}

        Se rendre dans l'edition pour ajouter des wilders et images


        <div class="form-group">
            <input class="btn btn-success btn-block" type="submit" value="Create"/>
        </div>
        {{ form_end(form) }}

        <a href="{{ path('project_index') }}" class="btn btn-primary">Retour à la liste</a>

    </div>

{% endblock %}

